export async function saveResult({ problem_id, status, output }) {
  //   await pool.query(
  //     `
  //     INSERT INTO submissions (user_id, problem_id, status, output, created_at)
  //     VALUES ($1, $2, $3, $4, NOW())
  //   `,
  //     [user_id, problem_id, status, output]
  //   );
}
